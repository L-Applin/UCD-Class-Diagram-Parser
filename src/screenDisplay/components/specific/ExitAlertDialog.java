package screenDisplay.components.specific;

import app.theme.AppTheme;
import screenDisplay.components.AlertDialogWithButton;

public class ExitAlertDialog extends AlertDialogWithButton {
    public ExitAlertDialog(String message, AppTheme theme) {
        super(message, theme);
    }
}
